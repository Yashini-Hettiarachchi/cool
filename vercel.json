{
  "buildCommand": "cd prototype && npm install && npm run build",
  "outputDirectory": "prototype/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
